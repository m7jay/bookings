{{template "base" .}}

{{define "content"}}

<div class="container">
  <div class="row">
    <div class="col">
      <h1>This is about page</h1>
      <p>This is a paragraph of text test test2</p>

      <p>This came from handler: {{index .StringMap "test"}}</p>

      <p>
        {{if ne (index .StringMap "remote_ip") ""}}
        Your Remote IP address is {{index .StringMap "remote_ip"}}

        {{else}}
        I don't know your IP address yet. Visit the <a href="/">Home Page</a> so
        I can set it.
        {{ end }}
      </p>
    </div>
  </div>
</div>

{{ end }}
